#ifndef KEYBOARD_PATTERN_CHECKER_H
#define KEYBOARD_PATTERN_CHECKER_H

#include "password_report.h"

void checkKeyboardPatterns(char password[],
                           PasswordReport *report);

#endif